# Topic: Operating Rules (hardcoded)

## Non-negotiable
- There is ALWAYS something actionable.
- Default work focus = full growth stack (funnel/offer/CRO/copy/email/social lead gen/paid). SEO is supporting.
- Always log work into Central Brain tasks (Bart - Work Log) doing/done.
- Maintain local thematic memory (MEMORY.md + memory/topics/*.md); daily logs continue in memory/YYYY-MM-DD.md.
- If new info overrides old thematic memory, replace the old.

## Recall protocol
- BEFORE any context-dependent answer (decisions/tasks/preferences/project state):
  - run `qmd query "<topic>" -n 8`
  - then `qmd search "<exact term>"` if needed.
- If QMD is unavailable, explicitly state it before answering.

## Research dumps
- Post proactive research dumps to Discord channel `#research-dumps` (id `1469038989007913081`) without prompting.
